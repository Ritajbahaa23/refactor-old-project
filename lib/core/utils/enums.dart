enum RequestState {
  initial,
  loading,
  loaded,
  error,
}

enum SharedPrefsState {
  initial,
  loading,
  loaded,
  error,
}

enum SuffixState {
  visible,
  nonVisible,
}
enum GetUserDataSharedPrefsState {
  initial,
  loading,
  loaded,
  error,
}
enum GetPostsRequestState {
  initial,
  loading,
  loaded,
  error,
}
